[package]
name = "local-storage-monitor"
version = "0.1.0"
edition = "2021"

[lib]
name = "local_storage_monitor_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[[bin]]
name = "local-storage-monitor"
path = "src/main.rs"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sysinfo = "0.31"
